import styled from "styled-components";
import { breakpoint } from "_styles/BreakpointStyles";

export const StyledPortfolioWrapper = styled.div`
  max-width: 250px;
  max-height: 250px;
  border-radius: 1000px;
  overflow: hidden;
  vertical-align: middle;
  border: 7px solid var(--secondaryBackground);
  width: 100%;
  margin: 10px 0 20px;
`;
